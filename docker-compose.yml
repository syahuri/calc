# =============================================================================
# Calc — Videotron Calculator
# React + nginx, no database
# =============================================================================
services:
  calc:
    build:
      context: ./source
      dockerfile: Dockerfile
    container_name: calc
    restart: unless-stopped
    networks:
      - npm_default

networks:
  npm_default:
    external: true